
<nav>
    <h2>Pick a color to get started</h2>
    <div class="navInputs">
        <input id="colorInput" type="color" class="colorInput" /> 
        <select class="color-dropBox" id="colorDropBox">
            <option value="monochrome">Monochrome</option>
            <option value="monochrome-light">Monochrome-light</option>
            <option value="monochrome-dark">Monochrome-dark</option>
            <option value="analogic">Analogic</option>
            <option value="complement">Complement</option>
            <option value="analogic-complement">Analogic-Complement</option>
            <option value="triad">Triad</option>
            <option value="quad">Quad</option>
        </select>
        <button id="getColorBtn">Get Color Scheme</button>
    </div>
</nav>

<style>

    h2 {
        padding: 0 0 10px;
        margin: 0;
        color: #D1D5DB;
        text-align: center;
    }

    nav {
        /* display: flex;
        flex-wrap: wrap;
        flex-direction: row;
        justify-content: center;
        gap: 10px; */
        padding: 20px 10px;
        background-color: #1F2937;
        box-shadow: 0 2px 5px;
    }

    .navInputs {
        display: flex;
        flex-wrap: wrap;
        width: 100%;
        justify-content: center;
        gap: 15px;
        /* display: grid;
        grid-template-columns: 60px 1fr;
        justify-content: center;
        gap: 25px */
    }

    #colorInput{
        border: none;
        max-width: 55px;
        width: 100%;
        height: 50px;
    }

    .color-dropBox{
        color: #D1D5DB;
        font-size: 18px;
        max-width: 220px;
        width: 100%;
        padding: 9px 13px;
        background: #1F293A;
        background-image: url("Images/Icon.svg");
        background-repeat: no-repeat;
        background-position: top 18px right 17px;
        border: 1px solid #D1D5DB;
        box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.05);
        border-radius: 6px;
        /* appearance: none; */
    }

    #getColorBtn{
        background: #3D4B60;
        color: white;
        font-size: 18px;
        box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.05);
        border: none;
        border-radius: 4px;
        width: 100%;
        max-width: 170px;
        height: 50px;
    }

    @media (width <= 700px) {
        #colorInput {
            width: 80%;
        }
    }

    @media (width >= 500px) {
        .navInputs {
            flex-wrap: nowrap;
            gap: 25px;
        }

        #colorInput {
            width: 80%;
        }

        .color-dropBox {
            max-width: 550px;
        }
    }
</style>